package main

import (
	"bufio"
	"os"
)

func main() {
	// calculator()
	// odd()
	// sum()
	// factorial()
	// minMaxArr()

	reader := bufio.NewReader(os.Stdin)
	structure(reader)
}
